/* Implement a circular buffer with write and read APIs */

#include <stdio.h>
#include <stdlib.h> /* for malloc etc. */

#define BUFFER_SIZE 10 /* Bytes */

int main()
{
  char *ptrToBuffer;
  ptrToBuffer = malloc(BUFFER_SIZE);

  memset(ptrToBuffer, 0x0);
  printf("\n%x\n", )
}
